diff --git a/ablit/fetch_transplant.py b/ablit/fetch_transplant.py
index a952633..652dc17 100644
--- a/ablit/fetch_transplant.py
+++ b/ablit/fetch_transplant.py
@@ -25,5 +25,6 @@ LAYER_MAP_PATH = REPO_ROOT / "ablit" / "LAYER_MAP.json"
 DONOR = os.environ.get("ABLIT_DONOR", "dealignai/GLM-5.3-Flash-UNCENSORED-NVFP4")
-BASE = f"https://huggingface.co/{DONOR}/resolve/main"
+DONOR_REVISION = os.environ.get("ABLIT_DONOR_REVISION", "main")
+BASE = f"https://huggingface.co/{DONOR}/resolve/{DONOR_REVISION}"
 TOKEN = os.environ.get("HF_TOKEN") or ""
 if not TOKEN:
     tok_file = Path.home() / ".cache/huggingface/token"
@@ -112,6 +113,6 @@ def main() -> None:
     manifest = {
         "donor": DONOR,
-        "donor_sha": None,
+        "donor_sha": DONOR_REVISION,
         "method": "dealign-oproj-transplant (byte-copy, o_proj L15-45 incl. MTP)",
         "tensors": {},
     }
